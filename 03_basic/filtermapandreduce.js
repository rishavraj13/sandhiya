 //For Each jo hai woh koi bhi value return nahi karwata hai 
//scope start karegay tho RETURN use karna hi parega

 const mynums = [1,5,7,5,43,78]
 const newnums = mynums.filter((num) => num > 4)
 console.log(newnums);
 